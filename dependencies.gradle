ext {
    agp_version = "4.0.0"
    lint_version = agp_version.split("[.]")[0].toInteger() + 23 + agp_version.substring(1)
    kotlin_version = '1.3.72'
    upload_lint_plugin = false
    upload_lint_library = false
    upload_lint_patch = true
}
